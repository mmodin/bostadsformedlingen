CREATE TABLE IF NOT EXISTS listings(
    id int PRIMARY KEY,
    fromDate text,
    toDate text,
    numberOf int,
    rooms int,
    isLoggedIn text,
    balcony text,
    fastQueue text,
    externalQueue text,
    address text,
    hasApplied text,
    hasGoodChance text,
    hasInternalQueue text,
    elevator text,
    rent real,
    internalQueue text,
    canApply text,
    queue text,
    queueName text,
    municipality text,
    latitude real,
    longitude real,
    short_term text,
    type text,
    similarStats real,
    Q1 real,
    Q3 real,
    newlyBuilt text,
    area text,
    senior text,
    district text,
    student text,
    youth text,
    url text,
    floor real,
    normal text,
    sqm real
)
